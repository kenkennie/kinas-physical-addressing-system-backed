# Application
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=username
DB_PASSWORD=password_secret
DB_NAME=your_db

# When running via Docker Compose, DB_PORT_HOST is the host-side mapped port
DB_PORT_HOST=5432

# Mapbox
MAPBOX_ACCESS_TOKEN=your_mapbox_token_here
